#!/bin/bash

sessionfile=${HOME}/session/updatesession

> $sessionfile
ssh-agent -s >> $sessionfile
dbus-launch --sh-syntax >> $sessionfile


#vars="SSH_CLIENT SSH_TTY SSH_AUTH_SOCK SSH_CONNECTION DISPLAY"

#for x in ${vars} ; do
#    (eval echo $x=\$$x) | sed  's/=/="/
#                                s/$/"/
#                                s/^/export /'
#done >${HOME}/session/updatesession

